const BananaStore=(()=>{const KEY='bananagis.farms.v1';function load(){try{return JSON.parse(localStorage.getItem(KEY)||'[]')}catch{return[]}}function save(farms){localStorage.setItem(KEY,JSON.stringify(farms));return farms}function merge(initial){const local=load();const ids=new Set(local.map(f=>f.properties?.farm_id));return [...initial,...local.filter(f=>!ids.has(f.properties?.farm_id))]}function exportGeoJSON(features){const blob=new Blob([JSON.stringify({type:'FeatureCollection',features},null,2)],{type:'application/geo+json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='banana-farms.geojson';a.click();URL.revokeObjectURL(a.href)}return{load,save,merge,exportGeoJSON}})();